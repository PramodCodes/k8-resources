apiVersion: v1
kind: Pod
metadata:
  name: hello-pod
  namspace: roboshop
spec:
# containers is a list of containers
  containers:
  - name: hello-pod
    image: nginx:1.14.2
    ports:
    - containerPort: 80
